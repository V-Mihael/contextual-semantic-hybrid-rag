[tool.poetry]
name = "contextual-rag-agno-supabase"
version = "0.1.0"
description = "Advanced RAG system with Contextual Retrieval, Hybrid Search, and Reranking"
authors = ["Your Name <your.email@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
agno = "^0.1.0"
supabase = "^2.0.0"
openai = "^1.0.0"
langchain = "^0.1.0"
langchain-text-splitters = "^0.0.1"
pypdf = "^3.17.0"
python-dotenv = "^1.0.0"
fastapi = "^0.109.0"
uvicorn = "^0.27.0"
pydantic = "^2.5.0"
pydantic-settings = "^2.1.0"

[tool.poetry.group.dev.dependencies]
jupyter = "^1.0.0"
ipykernel = "^6.28.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
